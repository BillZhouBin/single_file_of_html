# 像素精灵技术选型规则 · Sprite Ruleset

> 什么时候用「代码画的像素精灵（pixel sprite）」，什么时候该用图片 / SVG。
> 一句话：**代码画像素 = 画「风格化小色块」，不是画「图像」。**

---

## R0 总原则
能接受「硬边 + 受限调色 + 整张替换式帧动画」→ 进候选。
要渐变 / 曲线 / 抗锯齿 / 写实 → 直接排除。

## R1 尺寸三道红线（互不相关）

| 上限类型 | 硬天花板 | 越过会怎样 |
|---|---|---|
| 人肉手写矩阵 | **≤ 32×32** | 再大逐行敲百字符，对称/描边肉眼无法校对，出错率陡增 |
| 直接 `fillRect` 不预渲染 | 单精灵 ≤32px 且同屏 **几百级** | 每像素一次 fillRect，几百精灵就几十万次/秒，掉帧 |
| 预渲染 `bake` 到离屏 canvas 后 `drawImage` | **64–128px、同屏上千** | 只在初始化画一次，之后 GPU 贴图近乎零成本，天花板解除 |

经验取值：主角色 16–24px、小敌机/子弹 8–12px、Boss/立绘 **不要**用矩阵。

## R2 场景决策矩阵

| 维度 | 代码矩阵 ✅ | 美术 PNG ✅ | SVG ✅ |
|---|---|---|---|
| 风格 | 像素 / 复古 | 写实 / 精细 / 大尺寸 | 矢量 UI / 图标 |
| 动态 | 程序化换色、受击变红 | 固定美术帧 | 需平滑缩放 |
| 资源 | 单文件 / 零依赖 / 首包小 | 美术产出 sprite sheet | 图标系统导出 |
| 维护 | 程序直接改 | 美术在工具里改 | 设计稿导出 |

## R3 硬性排除（肯定别用代码矩阵）
- 写实 / 拟物 / 渐变 / 曲线的 UI 与插画（fillRect 做不出抗锯齿与贝塞尔）
- 全屏 Boss、风景背景、角色立绘等大尺寸高细节
- 骨骼 / 形变 / 流体粒子动画（矩阵只适合整张替换，做不了骨骼）
- 有专职像素美术的团队（程序手敲是倒退）
- 需 4K 大屏与手表都清晰的多尺寸响应式产品（除非你要的就是颗粒感）

## R4 性能红线
- 未预渲染时：每帧 fillRect 次数 ≈ 精灵数 × 像素数，**超 ~5 万次/帧必须优化**
- 命中红线 → 预渲染 bake + `drawImage`（GPU 加速，近乎零成本）
- 帧动画用「多矩阵整张替换」，不要用代码逐像素动画

## R5 一句话决策树
要**像素风 + 小 + 程序化改色**？→ 代码矩阵。
要**精细/大/写实**？→ PNG。
要**矢量/可缩放 UI**？→ SVG。

---

## 附：决策流图（文字版）

```
开始 · 选精灵实现方式
        │
        ▼
   风格主需求？
   ┌────┼────────┐
   │    │        │
像素·复古  写实·精细·大  矢量·可缩放
   │    │        │
   │    ▼        ▼
   │  美术 PNG    SVG 矢量
   │  /sprite sheet
   ▼
尺寸 ≤32 且需程序化改色？
   ┌────┐
是 │    │ 否
   ▼    ▼
代码像素矩阵  美术 PNG sheet
（推荐）
```

> 说明：本规则配套一张可视化决策流图（在对话中以 SVG 呈现）。文字版如上，便于离线速查。

---

## 本规则与飞机大战项目的对应
- `plane-war-game.html` 玩家战机 11×11、敌机 9×9：落在 R1「≤32×32 人肉手写」区间，无需预渲染，直接 fillRect 即可。
- 若后续加 Boss / 更精细角色：触发 R1 红线 → 改用「预渲染 bake + drawImage」。
- 若接专职像素美术 / 出精细立绘：走 R3 → 美术 PNG sprite sheet，UI 设计系统可原样复用。
- 子弹、爆炸粒子、小敌机、HUD 图标：符合 R2「程序化小元素」，继续用代码矩阵最划算。
